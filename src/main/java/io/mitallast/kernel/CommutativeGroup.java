package io.mitallast.kernel;

/**
 * An commutative group (also known as an abelian group) is a group
 * whose combine operation is commutative.
 */
public interface CommutativeGroup extends CommutativeMonoid, Group {
}